const Sidebar = {
    // sidebarWidth: 340
    sidebarWidth: 80
}

const reducerSidebar = (state = Sidebar, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default reducerSidebar